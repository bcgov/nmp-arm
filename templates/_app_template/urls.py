from django.conf.urls import patterns, url

urlpatterns = patterns( 'inout_board.app.views',

    #
    #  Examples
    #
    #url( r'^measure/history/(?P<client_id>[\w-]+)/$', ClientMeasureHistory.as_view(), name='client_measure_history' ),
    #url( r'^keep/alive/$', 'expiry_keep_alive', name='keep_session_alive' ),

)
